# frozen_string_literal: true

source 'https://rubygems.org'

# certvis itself is stdlib-only; these are only needed for `--serve` and
# for running the test suite.
gem 'webrick', '~> 1.9'

group :development do
  gem 'rspec', '~> 3.13'
end
